# My OPM Project

Live app: {{RAILWAY_URL}}

## Quick start

1. Ensure Docker is running.
2. Open your terminal in the project root and run:
   ```bash
   docker compose up --build
   ```
3. Access the frontend at `http://localhost:3000`.
